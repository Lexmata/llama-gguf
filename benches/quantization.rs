use criterion::{criterion_group, criterion_main, Criterion};

fn quantization_benchmark(_c: &mut Criterion) {
    // Placeholder for quantization benchmarks
}

criterion_group!(benches, quantization_benchmark);
criterion_main!(benches);
